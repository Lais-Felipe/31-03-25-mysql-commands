create database `lais`;
# a linha acima serve para criar um banco de dados com o nome`lais`
# o uso da crase se torna necessário para indicar um `string` no sistema
# `string`do sistema `database,`table` e `campo`